
{
    int x = 14;
    int y = 12;
    int r = x % y;
    int z = x * y;

    while (r != 0) {
       x = y;
       y = r;
       r = x % y;
    }

    sprint("result: ");
    iprint(z / y);
    sprint("\n");
}

