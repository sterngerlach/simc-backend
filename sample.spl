{
    int a, b, m, n, r;
    int[2] inputs;

    sprint("You must give 2 integers.\n");

    new(inputs);

    sprint("First integer: ");
    scan(inputs[0]);
    sprint ("Second integer: ");
    scan(inputs[1]);

    m = a;
    n = b;
    r = m - (m / n) * n;
    m = n;
    n = r;

    while (r > 0) {
        r = m - (m / n) * n;
        m = n;
        n = r;
    }

    sprint("Answer = ");
    iprint(m) ;
    sprint("\n");
}

